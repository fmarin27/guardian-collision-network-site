--- a/components/ConciergeBar.tsx	2026-03-03 04:27:59.139022889 +0000
+++ b/components/ConciergeBar.tsx	2026-03-03 04:28:00.050862678 +0000
@@ -2,7 +2,7 @@
 
 import { useCallback, useEffect, useMemo, useRef, useState, type ChangeEvent } from "react";
 
-import { collection, onSnapshot, orderBy, query, addDoc, serverTimestamp } from "firebase/firestore";
+import { collection, onSnapshot, orderBy, query, addDoc, serverTimestamp, doc, setDoc } from "firebase/firestore";
 import { db } from "@/app/lib/firebase";
 
 
@@ -579,6 +579,17 @@
   setMessages((prev) => [...prev, { from: "user", text: trimmed }]);
 
   try {
+    // ✅ Ensure parent session doc exists (prevents 'document does not exist' in console UI)
+    await setDoc(
+      doc(db, "conciergeSessions", sid),
+      {
+        sessionCode: sid.replace(/^UAB-/, ""),
+        lastMessageAt: serverTimestamp(),
+        status: "LIVE",
+      },
+      { merge: true }
+    );
+
     await addDoc(collection(db, "conciergeSessions", sid, "messages"), {
       who: "user",
       text: trimmed,
@@ -850,7 +861,7 @@
     setPendingMode(null);
   }
 
-  function resetChat(nextMode: ConciergeMode = "default") {
+  function resetChat(nextMode: ConciergeMode = "default", opts?: { newSession?: boolean }) {
     setMode(nextMode);
 
     setIntent("other");
@@ -899,10 +910,13 @@
     setExtraNotes([]);
     lastFilePickSigRef.current = "";
 
-    notifiedRef.current = false;
-    sessionIdRef.current = makeSessionId();
-    try { window.localStorage.setItem(UAB_SESSION_ID_KEY, sessionIdRef.current); } catch {}
-    setSessionId(sessionIdRef.current);
+    const wantsNewSession = !!opts?.newSession;
+    if (wantsNewSession) {
+      notifiedRef.current = false;
+      sessionIdRef.current = makeSessionId();
+      try { window.localStorage.setItem(UAB_SESSION_ID_KEY, sessionIdRef.current); } catch {}
+      setSessionId(sessionIdRef.current);
+    }
     managerSeenIdsRef.current = new Set();
 
     handoffStageRef.current = "idle";
@@ -2491,7 +2505,7 @@
 
                   <button
                     type="button"
-                    onClick={() => resetChat(mode)}
+                    onClick={() => resetChat(mode, { newSession: true })}
                     className="text-xs text-white/70 hover:text-white transition px-3 py-2 rounded-lg border border-white/10 hover:border-white/20"
                   >
                     Reset
